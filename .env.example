# Vite does not need public environment variables for the standard Bankgraph build.
#
# Cloudflare Worker secrets belong in `.dev.vars` for local work and in Wrangler's
# encrypted secret store for production. Copy `.dev.vars.example` to `.dev.vars`.
